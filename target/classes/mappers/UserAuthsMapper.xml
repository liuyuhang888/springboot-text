<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.offcn.mapper.UserAuthsMapper">
    <select id="findByUserId" resultType="com.offcn.entiry.UserAuths">
        select * from user_auths where user_id = #{userId}
    </select>
  </mapper>